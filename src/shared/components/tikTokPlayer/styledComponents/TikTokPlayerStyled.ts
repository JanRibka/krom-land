import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

const TikTokPlayerStyled = styled(Box)(({ theme }) => ({
  display: "flex",
  justifyContent: "center",
}));

export default TikTokPlayerStyled;
