import Box from '@mui/material/Box';
import { styled } from '@mui/material/styles';

const GoogleMapsStyled = styled(Box)(({ theme }) => ({
  height: "400px",
  width: "100%",
  marginBottom: "50px",
}));

export default GoogleMapsStyled;
