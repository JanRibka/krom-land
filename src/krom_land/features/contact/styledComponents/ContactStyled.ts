import Stack from '@mui/material/Stack';
import { styled } from '@mui/material/styles';

const ConstctStyled = styled(Stack)(({ theme }) => ({
  marginBottom: "80px",
}));

export default ConstctStyled;
